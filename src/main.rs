mod compressor;
mod dictionary;
mod in_memory_dict;
mod indexer;
mod my_bk_tree;
mod positional_intersect;
mod query_parser;
mod query_processor;
mod scoring;
pub mod search_engine;
mod utils;
fn main() -> Result<(), Box<dyn std::error::Error>> {
    Ok(())
}
